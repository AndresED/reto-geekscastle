## Context

Archive moved change under `openspec/changes/archive/2026-07-15-bootstrap-users-api/`. Specs live in `openspec/specs/`.

## Decisions

- README table: primary link `openspec/specs/`; optional archive link.
- Purpose one-liners derived from archived proposal capabilities.
- Do not mark ADR-0005 throttle/Helmet done until `harden-api-security-baseline` applies — instead enmienda “pending sibling change” or leave unchecked with note.

## Open Questions

- None material.
